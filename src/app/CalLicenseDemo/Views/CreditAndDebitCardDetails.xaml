<Page x:Class="CalLicenseDemo.CreditAndDebitCardDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CalLicenseDemo.Views"
             mc:Ignorable="d" Background="Black">
    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/DefaultStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
       
    </Page.Resources>
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type TextBox}" >
                <Setter Property="Margin" Value="20"></Setter>
            </Style>
            <Style TargetType="{x:Type ComboBox}" >
                <Setter Property="Margin" Value="20"></Setter>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
            <ColumnDefinition  />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label x:Name="label" Content="16 digit card number" FontWeight="Bold"  Foreground="White" Grid.Row="0"/>
        <TextBox x:Name="textBox" TextWrapping="Wrap" Text="" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2"/>
        <Label x:Name="lblExpiryDate" Content="Expiry Date"  FontWeight="Bold" Foreground="White" Grid.Row="1" Grid.Column="0" />
        <ComboBox x:Name="ListofMonths" Text="Month"  Grid.Row="1" Grid.Column="1" />
        <ComboBox x:Name="listOfYears" IsSynchronizedWithCurrentItem="True" ScrollViewer.VerticalScrollBarVisibility="Visible" Grid.Row="1" Grid.Column="2"/>
        <Label x:Name="lblCVV" Content="CVV"   DockPanel.Dock="Right" FontWeight="Bold" Grid.Row="2" Foreground="White" Grid.Column="0" />
        <TextBox  x:Name="txtCVV"  Grid.Row="2" Grid.Column="1" />
        <Label Content="Name on the card" FontWeight="Bold"  FontStyle="Oblique" Foreground="White" Grid.Row="3" Grid.Column="0"/>
        <TextBox Text="Name" Foreground="LightGray" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" />
        <Button Style="{StaticResource DefaultButtonStyle}" x:Name="button" Content="PURCHASE" Background="LightBlue" Click="button_Click" Grid.ColumnSpan="3"  Grid.Row="4"  />
    </Grid>
</Page>
